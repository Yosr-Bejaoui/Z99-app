### `BackgroundRemoverScreen.tsx`
- Has a malformed closing tag syntax like `<View}` instead of `<View>`.
- Missing `<SafeAreaView>` wrapper causing layout to break on modern devices.

### `ImageEditorScreen.tsx`
- Missing `<ScreenHeader>` entirely.
- Has a malformed closing tag syntax like `<View}` instead of `<View>`.
- Missing `<SafeAreaView>` wrapper causing layout to break on modern devices.

### `ImageGenScreen.tsx`
- Missing `<ScreenHeader>` entirely.
- Has a malformed closing tag syntax like `<View}` instead of `<View>`.
- Missing `<SafeAreaView>` wrapper causing layout to break on modern devices.

### `ImageTo3DScreen.tsx`
- Missing `<ScreenHeader>` entirely.
- Has a malformed closing tag syntax like `<View}` instead of `<View>`.

### `ImageToVideoScreen.tsx`
- Missing `<ScreenHeader>` entirely.
- Has a malformed closing tag syntax like `<View}` instead of `<View>`.

### `ImageUpscalerScreen.tsx`
- Contains hardcoded `"coins"` instead of `"credits"` (e.g. `Alert.alert` or `showToast`).
- Has a malformed closing tag syntax like `<View}` instead of `<View>`.
- Missing `<SafeAreaView>` wrapper causing layout to break on modern devices.

### `SpeechToTextScreen.tsx`
- Missing `<ScreenHeader>` entirely.
- Contains hardcoded `"coins"` instead of `"credits"` (e.g. `Alert.alert` or `showToast`).
- Has a malformed closing tag syntax like `<View}` instead of `<View>`.
- Missing `<SafeAreaView>` wrapper causing layout to break on modern devices.

### `TextToSpeechScreen.tsx`
- Has a malformed closing tag syntax like `<View}` instead of `<View>`.

### `TextToVideoScreen.tsx`
- Missing `<ScreenHeader>` entirely.
- Has a malformed closing tag syntax like `<View}` instead of `<View>`.

### `VideoEffectsScreen.tsx`
- Missing `<ScreenHeader>` entirely.
- Has a malformed closing tag syntax like `<View}` instead of `<View>`.

### `VideoUpscalerScreen.tsx`
- Contains hardcoded `"coins"` instead of `"credits"` (e.g. `Alert.alert` or `showToast`).
- Contains stray BOM/strange characters (e.g. `ï»¿`).
- Has a malformed closing tag syntax like `<View}` instead of `<View>`.

### `VoiceCloningScreen.tsx`
- Contains hardcoded `"coins"` instead of `"credits"` (e.g. `Alert.alert` or `showToast`).
- Has a malformed closing tag syntax like `<View}` instead of `<View>`.
- Missing `<SafeAreaView>` wrapper causing layout to break on modern devices.