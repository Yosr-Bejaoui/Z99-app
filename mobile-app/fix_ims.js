const fs=require('fs'); let glob = fs.readdirSync('src/screens').filter(f=>f.endsWith('.tsx')).map(f=>'src/screens/'+f); for(let f of glob){let c=fs.readFileSync(f,'utf8'); if(c.includes('import { spacing } from \\'../theme/tokens\\';')){ fs.writeFileSync(f, c.replace('import { spacing } from \\'../theme/tokens\\';', 'import { spacing } from \\'../theme/colors\\';')); console.log('Fixed', f); } }
